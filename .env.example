# Copy to .env (gitignored) and fill in. Never commit real secrets (SECURITY-12).
# Use sslmode=require or higher in non-local environments (SECURITY-01).
MINIAIP_DATABASE_URL=postgresql://miniaip:miniaip@localhost:5432/mini_aip
